// SD
// Componentes
// Button (Botón) - Pruebas
// José Esteva <josesteva@soft4pilot.net>
// 

import { render } from '@testing-library/react';
import Button from './index';

test('Componente <Button/>', () => {
  render(<Button/>);
});
