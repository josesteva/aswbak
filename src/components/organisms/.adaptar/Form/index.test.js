// SD
// Pruebas
// Form (Formulario)
// José Esteva <josesteva@cic.unam.mx>

import { render } from '@testing-library/react';

import Form from './index';

test('Componente <Form/>', () => {
  render(<Form/>);
});
